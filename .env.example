# (Tùy chọn, chỉ tiện khi dev) Copy thành .env rồi điền key Gemini/Veo.
# Khi chạy thật, người dùng nhập key qua popup "API Key" (lưu localStorage).
# Veo dùng chung Google AI API key với Gemini (lấy ở aistudio.google.com/apikey).
VITE_GEMINI_API_KEY=
